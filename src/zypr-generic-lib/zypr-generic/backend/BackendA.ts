import { Backend } from "../Backend";
import "../../../TypeCraft/Typescript/Backend"

// uses the iteroperations defined in TypeCraft/Typescript/Backend to build a
// backend that calls the purescript backenbd
export const backend: Backend = (() => { throw new Error("TODO") })()